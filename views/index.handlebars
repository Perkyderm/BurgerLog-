<h1>Burgers!</h1>

<h2>Burgeres ready to be eaten!</h2>

<ul>
  {{#each burgers}}
  {{#unless devoure}}
  {{> burgers/burger-block devoure=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that you Devoured!</h2>

<ul>
  {{#each burgers}}
  {{#if devoure}}
  {{> burgers/burger-block devoure=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form id="create-form">

  <div class="form-group">
    <label for="ca">Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoure">Was this already devoured, or add it to the menu?</label><br>
    <input type="radio" name="devoure" value="1" id="devoure"> Devoured!<br>
    <input type="radio" name="devoure" value="0" id="Menu" checked> Menu
  </div>

  <button type="submit">Add Burger</button>
</form>